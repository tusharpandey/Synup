package com.app.synuptest.model.repo.repoImplementation;

import com.app.synuptest.model.repo.DBRepository;

import retrofit2.Callback;

public class DBRepositoryImplementation implements DBRepository {
    @Override
    public void getPizzaList(Callback callback) {
    }
}
